{% extends 'base.html.twig' %}

{% block body %}
<div class="col-12 grid-margin stretch-card">
    <div class="card">
      <div class="card-body">

        <h4 class="card-title">Basic form elements</h4>
            <p class="card-description">
                Basic form elements
                </p>
                {{ form_start(form, {'attr': {'class': 'form-sample'}}) }}
                    <div class="form-group">
                        {{ form_row(form.nom,  {'attr': {'class': 'form-control',id 'placeholder': 'Enter your name'}}) }}
                    </div>
                    <div class="form-group">
                        {{ form_row(form.prenom,  {'attr': {'class': 'form-control',id 'placeholder': 'Enter your name'}}) }}
                    </div>
                    <div class="form-group">
                        {{ form_row(form.numero,  {'attr': {'class': 'form-control',id 'placeholder': 'Enter your name'}}) }}
                    </div>
                    <div class="form-group">
                        {{ form_row(form.email,  {'attr': {'class': 'form-control',id 'placeholder': 'Enter your name'}}) }}
                    </div>
                {{ form_end(form) }}    

        </div> 
    </div> 
</div>        
{% endblock %}
