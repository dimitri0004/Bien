{% extends 'base.html.twig' %}
{% block title %} Ajouter Un Agent Immobilier{% endblock %}
{% block body %}
<div class="col-12 grid-margin stretch-card">
    <div class="card">
      <div class="card-body">
            {% for message in app.flashes('success') %}
                <div class="alert alert-success">
                    {{ message }}
                </div>
            {% endfor %}
        <h4 class="card-title">Les Information Du Locataire</h4>
            <p class="card-description">
              Information Primaire
                </p>
                <a class="btn btn-success" href="{{ path('locataire_afficher') }}" >Liste des Locataires</a>
                {{ form_start(form, {'attr': {'class': 'form-floating mt-2'}}) }}
                    <div class="form-group">
                        {{ form_row(form.nom,  {'attr': {'class': 'form-control', 'placeholder': 'Enter votre nom'}}) }}
                    </div>
                    <div class="form-group">
                        {{ form_row(form.prenom,  {'attr': {'class': 'form-control', 'placeholder': 'Enter votre prenom'}}) }}
                    </div>
                    <div class="form-group">
                        {{ form_row(form.numero,  {'attr': {'class': 'form-control', 'placeholder': 'Enter votre numero'}}) }}
                    </div>
                    <div class="form-group">
                        {{ form_row(form.age,  {'attr': {'class': 'form-control', 'placeholder': 'Enter votre age'}}) }}
                    </div>
                    <div class="form-group">
                        {{ form_row(form.Propreitaire,  {'attr': {'class': 'form-control', 'placeholder': 'Propreitaire'}}) }}
                    </div>
                    <div class="form-group">
                        {{ form_row(form.BienImmobilier,  {'attr': {'class': 'form-control', 'placeholder': 'Bien Immobilier'}}) }}
                    </div>
                    <button type="submit" class="btn btn-primary me-2">Modifier</button>
                {{ form_end(form) }}    

        </div> 
    </div> 
</div>        
{% endblock %}
